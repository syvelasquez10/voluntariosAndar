<%= form_with(model: job, local: true) do |form| %>
  <% if job.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(job.errors.count, "error") %> prohibited this job from being saved:</h2>

      <ul>
      <% job.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :volunteer_id %>
    <%= form.number_field :volunteer_id, id: :job_volunteer_id %>
  </div>

  <div class="field">
    <%= form.label :role_id %>
    <%= form.number_field :role_id, id: :job_role_id %>
  </div>

  <div class="field">
    <%= form.label :initial_date %>
    <%= form.date_select :initial_date, id: :job_initial_date %>
  </div>

  <div class="field">
    <%= form.label :final_date %>
    <%= form.date_select :final_date, id: :job_final_date %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
